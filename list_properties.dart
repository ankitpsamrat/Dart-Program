void main(List<String> args) {
  List s = ['Samrat', 'Rohit', 'Raja'];

  // to check the length of list
  // print(s.length);

  // to reverse the value of list
  // print(s.reversed);

  // to find the first value from list
  // print(s.first);

  // to find the last value from list
  // print(s.last);

  // to remove any value from list
  // s.remove('Rohit');
  // print(s);

  // to remove any value using index of list
  // s.removeAt(0);
  // print(s);

  // to remove last value from list
  // s.removeLast();
  // print(s);

  // to remove all value from list
  s.clear();
  print(s);
}
