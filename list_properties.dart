void main(List<String> args) {
  List s = ['Samrat', 'Rohit', 'Raja'];

  //  for checking the length of list
  // print(s.length);

  //  for reverse the value of list
  // print(s.reversed);

  //  for find the first value from list
  // print(s.first);

  //  for find the last value from list
  // print(s.last);

  //  for remove any value from list
  // s.remove('Rohit');
  // print(s);

  //  for remove any value from specific location from lis
  // s.removeAt(0);
  // print(s);

  //  for remove last value from list
  s.removeLast();
  print(s);
}
